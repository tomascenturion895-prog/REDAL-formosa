// Helpers para TOTP - Las funciones de generación y verificación
// se implementarán via Edge Functions de Supabase en futuro

// Por ahora, estos son placeholders para las funciones que se necesitarán:
// - generateTOTPSecret(userId): genera un nuevo secret y retorna QR code
// - verifyTOTPCode(userId, code): verifica un código TOTP válido
// - enableTOTP(userId): marca TOTP como habilitado
// - disableTOTP(userId): desactiva TOTP para el usuario

export function placeholder() {
  // Placeholder para evitar archivo vacío
}
